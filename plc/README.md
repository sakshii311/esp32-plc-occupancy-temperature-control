# PLC project — pending

The Bosch Rexroth IndraLogic project files are **not yet in this repo**.

IndraLogic is only installed on the lab machine, not on the repo owner's
personal machine, so the project could not be exported and committed at
repo-setup time. These files will be added after the next lab session.

A rung-overview sketch of the intended ladder logic exists (see
[`../docs/`](../docs/)), but it is a template/placeholder — the actual
timer presets, contact names, and rung structure need to be exported from
IndraLogic and added here, not guessed from the sketch.

**To do:** after the next lab session, add:

- The IndraLogic project file(s) for the ladder logic program
- A real ladder logic export (image or PDF) to replace
  `docs/ladder_logic_PLACEHOLDER.svg`
